/* Challenge

Write a script that initializes a variable age with the value 18.

After that, initialize another variable canVote that will hold whether the person is old enough to vote (18 or older). */

// Type your code below
let age = 18;
let canVote = age >= 18;

// Don't change the line below
console.log(`age = ${age}, canVote = ${canVote}`);
