/* Challenge

Write a code that initializes two variables, x and y, with the values 12 and 3 (respectively).

After that, initialize another variable z that will hold the multiplication of them. */

// Type your code below
let x = 12;
let y = 3;
let z = x * y;

// Don't change the line below
console.log(`x = ${x}, y = ${y}, z = ${z}`);
