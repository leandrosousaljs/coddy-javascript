/* Challenge

Write a code that initializes two variables, x and y, with the values 15 and 4 (respectively).

After that, initialize another variable z that will hold the result of x modulo y. */

// Type your code below
let x = 15;
let y = 4;
let z = x % y;

// Don't change the line below
console.log(`x = ${x}, y = ${y}, z = ${z}`);
