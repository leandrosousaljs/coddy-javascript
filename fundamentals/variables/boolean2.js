/* Challenge

Declare a variable named isLoggedIn and assign it the value false to represent that a user is not currently logged in. */

// Type your code below
let isLoggedIn = false;

// Don't change the line below
console.log(`User logged in: ${isLoggedIn}`);
