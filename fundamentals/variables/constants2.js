/* Challenge

Initialize a constant variable named maxSpeed with the value of 120 */

// Type your code below
const maxSpeed = 120;

// Don't change the line below
console.log(`maxSpeed = ${maxSpeed}`);
