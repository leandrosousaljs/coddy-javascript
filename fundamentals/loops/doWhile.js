/* Challenge

Write a do...while loop that starts with a variable count set to 5. The loop should:

Print the current value of count.
Decrease count by 1 after each iteration.
As long as count is bigger than 0 */

let count = 5;
do {
  console.log(count);
  count--;
} while (count > 0);
