/* Challenge

Write a function called printMessage that:

Prints the message: "This is a reusable function!".
Call the printMessage function 3 times in your program. */

// Write code here

function printMessage() {
  console.log('This is a reusable function!');
}

printMessage();
printMessage();
printMessage();
