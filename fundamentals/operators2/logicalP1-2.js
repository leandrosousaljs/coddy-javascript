/* Challenge

You are given a code. Replace the question marks of the variables b1, b2 and b3 so that b4 holds true. */

// Type your code below
let b1 = true;
let b2 = true;
let b3 = true;

// Don't change below
let b4 = b1 && b2 && b3;
console.log(`b4 = ${b4}`);
