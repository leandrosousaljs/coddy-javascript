/* Challenge

You are given a code with a logical expression. Replace the question marks of the variables x and y so that z evaluates to true. */

// Type your code below
let x = 5;
let y = -2;
let z = x * y < x + y && x + y < x - y;

// Don't change the line below
console.log(`z = ${z}`);
