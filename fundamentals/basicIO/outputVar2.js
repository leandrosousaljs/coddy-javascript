/* Challenge

You are given a code that stores a random number as input to a variable named rnd.

Print "The input is: " followed by the value of rnd.
Print:
"The number is even." if rnd is even.
"The number is odd." if rnd is odd. */

let rnd = inp; // Don't change this line

console.log(`The input is: ${inp}`);
console.log(inp % 2 == 0 ? 'The number is even.' : 'The number is odd.');
