/* Challenge

Create a function named firstAndLast that:

Takes one argument: arr (an array).
Returns the addition of the first and last elements of the array. */

// Write code here

let firstAndLast = arr => {
  return arr[0] + arr[arr.length - 1];
};
